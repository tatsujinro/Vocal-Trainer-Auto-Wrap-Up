name: 自動打包聲樂教練 HTML

# 觸發條件：當 main 分支有代碼推送時
on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # 允許手動按按鈕觸發

permissions:
  contents: write # 允許機器人修改倉庫內容

jobs:
  build:
    runs-on: ubuntu-latest # 使用 Ubuntu 虛擬機

    steps:
      # 1. 把倉庫的代碼抓下來
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. 安裝 Python 環境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # 3. 執行你的 Python 打包腳本
      - name: Run Python Builder
        run: |
          # 這裡填寫你上傳的 Python 檔名，例如 build_offline_app.py
          python build_offline_app.py

# 4. 部署與封存版本
      - name: Deploy and Archive Version
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"

          # 🔴 這裡一定要改！要跟 Python 腳本產出的檔名一模一樣！
          TARGET_FILE="VocalTrainer_Offline_v27_2.html"

          # (這行是舊的，記得改成上面的 v27_2)
          # TARGET_FILE="VocalTrainer_Offline_v27_1.html" 

          # --- 以下不用動 ---
          if [ -f "$TARGET_FILE" ]; then
            echo "✅ 檢測到目標檔案: $TARGET_FILE"
          else
            echo "❌ 錯誤：找不到 $TARGET_FILE ！Python 建置可能失敗了。"
            ls -R
            exit 1
          fi

          cp -f "$TARGET_FILE" index.html

          git add "$TARGET_FILE"
          git add index.html

          git commit -m "Auto-deploy: $TARGET_FILE" || echo "⚠️ 沒有變更可提交"
          git push
